//libraries and hooks
//components
//assets and styled components
import { Logo } from "./Logo";

export const Header = () => {
   return (
      <header className="Header">
         <Logo />
      </header>
   );
};