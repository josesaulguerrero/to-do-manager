//libraries and hooks
//components
//assets and styled components

export const Logo = ({ className, children }) => {
   return (
      <h1 className={className}>
         {children}
      </h1>
   );
};